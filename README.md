# simplest-todo

Minimal Next.js todo app with local storage.

## Quick start

- **Node.js** 18+ required
- `npm install`
- `npm run dev` → http://localhost:3000
- `npm run build` to build for production
